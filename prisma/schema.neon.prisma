// This is the Neon PostgreSQL version of the Prisma schema.
// Copy this file to schema.prisma when deploying to Vercel + Neon.
//
// Steps to deploy:
// 1. Copy this file: cp prisma/schema.neon.prisma prisma/schema.prisma
// 2. Set DATABASE_URL in Vercel environment variables to your Neon connection string
// 3. Run: bun run db:migrate:deploy (or let Vercel build do it via postinstall)
// 4. Deploy: vercel deploy

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Crew {
  id          String      @id @default(cuid())
  namaCrew    String
  fotoUrl     String
  idKaryawan  String      @unique
  pin         String      @default("0000")
  groupId     String?
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  salesData   SalesData[]
  group       Group?      @relation(fields: [groupId], references: [id])

  @@index([groupId])
  @@index([idKaryawan])
}

model Group {
  id              String   @id @default(cuid())
  namaGrup        String
  logoUrl         String
  targetBulanan   Float    @default(0)
  week1Percentage Float    @default(25)
  week2Percentage Float    @default(25)
  week3Percentage Float    @default(25)
  week4Percentage Float    @default(25)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  crews           Crew[]
}

model SalesData {
  id               String   @id @default(cuid())
  tanggal          DateTime
  idPenjualan      String
  statusRetention  String?
  retentionCode    String?
  pembayaran       String?
  program          String?
  channelStock     String?
  kodeExtend       String
  brand            String?
  dept             String?
  modul            String?
  ukuran           String?
  qty              Int
  hjp              Float?
  netto            Float?
  diskon           Float?
  diskonRp         Float?
  potongan         Float?
  potonganV        Float?
  settle           Float
  crewId           String?
  importBatchId    String
  crew             Crew?      @relation(fields: [crewId], references: [id])
  importBatch      ImportBatch @relation(fields: [importBatchId], references: [id])
  createdAt        DateTime   @default(now())
  updatedAt        DateTime   @updatedAt

  @@index([crewId])
  @@index([importBatchId])
  @@index([tanggal])
  @@index([kodeExtend])
}

model ImportBatch {
  id           String      @id @default(cuid())
  fileName     String
  totalRecords Int
  importDate   DateTime
  createdAt    DateTime    @default(now())
  salesData    SalesData[]
}
